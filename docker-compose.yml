version: '3'

services:
  inquizitive-app-container:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: inquizitive-app-container
    restart: always
    ports:
      - "8000:3000"

